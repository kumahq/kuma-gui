# x-router

Renderless component to install top-level anchor/click listener.

This is installed automatically with all XAction and XI18n components.

The only reason to install/add it it manually is if there is in the rare chance
that you have a page that needs to route that doesn't have a XAction nor XI18n

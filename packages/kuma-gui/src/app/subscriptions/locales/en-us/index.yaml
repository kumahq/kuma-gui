subscriptions:
  notifications:
    with-nacks: !!text/markdown |
      The Global Control Plane got NACK responses during this subscription

  routes:
    item:
      config: Configuration
      format: Format
      formats:
        yaml: YAML
        structured: Structured
      navigation:
        zone-cp-subscription-summary-overview-view: Overview
        zone-cp-subscription-summary-config-view: YAML
        zone-ingress-subscription-summary-overview-view: Overview
        zone-ingress-subscription-summary-config-view: YAML
        zone-egress-subscription-summary-overview-view: Overview
        zone-egress-subscription-summary-config-view: YAML
        data-plane-subscription-summary-overview-view: Overview
        data-plane-subscription-summary-config-view: YAML
      headers:
        config: Config
        responses: Total Responses (sent/ack'ed)
        type: Type
        stat: Responses sent/ack'ed
        nacked: Responses rejected / NACK'ed

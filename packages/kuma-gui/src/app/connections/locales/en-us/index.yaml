inbounds:
  x-empty-state:
    title: No inbounds
outbounds:
  x-empty-state:
    title: No active outbounds
connections:
  include_endpoints: Include Endpoints
  routes:
    item:
      navigation:
        overview: 'Overview'
        xds: 'XDS Configuration'
        stats: 'Stats'
        clusters: 'Clusters'

overlay: 1.0.0
info:
  title: Overlay to customise API for Kuma
  version: 0.1.0
extends: ./dist/openapi.yaml
actions:
  # versioning
  - target: "$.info"
    update:
      x-oas-source: kumahq/kuma@${GIT_SHA}

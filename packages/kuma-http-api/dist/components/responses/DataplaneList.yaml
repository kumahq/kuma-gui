description: List
content:
  application/json:
    schema:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '../schemas/DataplaneItem.yaml'
        next:
          description: URL to the next page, or null when this is the last page
          type:
            - string
            - 'null'
        total:
          description: The total number of entities
          type: number
      required:
        - total
        - items
        - next

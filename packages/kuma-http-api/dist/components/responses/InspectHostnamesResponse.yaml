description: A response containing hostnames that match a service.
content:
  application/json:
    schema:
      $ref: "../schemas/InspectHostnames.yaml"
    examples:
      ResponseForHostnames:
        $ref: "../examples/InspectHostnamesExample.yaml"

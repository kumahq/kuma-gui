description: A response containing a list of mesh insights.
content:
  application/json:
    schema:
      type: object
      properties:
        total:
          type: integer
          example: 200
        next:
          type: string
        items:
          type: array
          items:
            $ref: '../schemas/MeshInsightWithMeta.yaml'

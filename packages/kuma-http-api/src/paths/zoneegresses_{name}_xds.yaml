get:
  parameters:
    - name: name
      type: string
      in: path
      required: true
    - name: include_eds
      type: string
      in: query
      required: false
  responses:
    200:
      description: '200'
      content:
        application/json:
          schema:
            type: object


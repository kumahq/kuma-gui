get:
  parameters:
    - name: name
      type: string
      in: path
      required: true
  responses:
    200:
      description: '200'
      content:
        text/plain:
          schema:
            type: string


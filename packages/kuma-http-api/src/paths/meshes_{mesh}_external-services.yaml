get:
  parameters:
    - name: mesh
      type: string
      in: path
      required: true
    - in: query
      name: tag
    - $ref: '../parameters.yaml#/size'
    - $ref: '../parameters.yaml#/offset'
    - $ref: '../parameters.yaml#/filter'
  responses:
    200:
      description: '200'
      content:
        application/json:
          schema:
            type: object

